[tox]
envlist = th
#skipsdist = True

[testenv]
deps =
#    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements-evernote.txt

setenv =
         DJANGO_SETTINGS_MODULE=django_th.settings
         PYTHONPATH={toxinidir}
         PYTHONHASHSEED=0

commands = python manage.py test django_th th_rss -v2

